// @flow

import * as React from 'react';
import { View } from 'react-native';

// Find an Icon component in '@kiwicom/universal-components'
// It accepts a name props with value "plus"

type Props = {|
  // TODO
|};

const DashedBox = (props: Props) => {
  return <View>{/** TODO */}</View>;
};

export default DashedBox;
